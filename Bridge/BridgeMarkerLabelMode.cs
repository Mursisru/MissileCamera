namespace MissileCamera.Bridge
{
    // Which HUD unit names are sent in bridge marker JSON (browser NOXMFD overlay).
    internal enum BridgeMarkerLabelMode
    {
        All,
        SelectedOnly,
        None
    }
}
